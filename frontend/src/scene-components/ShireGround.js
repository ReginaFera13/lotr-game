import { usePlane } from "@react-three/cannon"

export const ShireGround = () => {
    const [ref] = usePlane(() => ({
        rotation: [0,0,0],
        position: [0,0,0]
    }))

    return (
        
    )
}